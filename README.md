# setup_dev_env
  
  
```
sudo apt-get install build-essential git wget curl vim zip lsb-release cmake make autoconf libtool software-properties-common python3-dev zsh apt-transport-https ca-certificates ssh ftpd freeglut3
  
sudo apt-get install net-tools pinta htop tmux nmon ncdu tree ntp
```
